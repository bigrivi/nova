export const DRAFT_THREAD_ID = "__draft__";
// Mirrors Tailwind's md breakpoint (48rem) but strictly below it, so exactly
// one of "narrow JS state" and "md: CSS layout" applies at every width.
export const NARROW_VIEWPORT_QUERY = "(width < 48rem)";
export const SELECTED_AGENT_KEY_STORAGE = "nova:selectedAgentKey";
export const DEFAULT_AGENT_KEY = "main";
